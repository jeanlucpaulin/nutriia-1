<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission pour accéder à Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nutriia"
        tools:targetApi="31">

        <activity
            android:name=".activities.AppLaunchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ObjectifActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.MainActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.HealthInformationActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.FormationActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.DishCompositionActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.FoodCompositionActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.CoachActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.RedefineGoalActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.DayAnalysisActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.LoginActivity"
            android:exported="true">
        </activity>

        <!-- Déclaration de MeetActivity -->
        <activity
            android:name=".activities.MeetActivity"
            android:exported="true">
        </activity>

        <activity android:name=".activities.WebViewActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>